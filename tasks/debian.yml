---

- name: create locales
  locale_gen:
    name: "{{ locale }}"
    state: present
  become: yes

- name: update-locale
  command: update-locale LC_ALL={{ locale }} LANG={{ locale }}
  become: yes

- set_fact:
    ssh_service_name: ssh
